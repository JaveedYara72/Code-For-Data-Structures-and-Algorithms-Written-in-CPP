bool compare(int a, int b) {
	return a > b; //Descending Order
}

bool compare(int a, int b){
  return a<b; // Ascending Order
 }

int a[1000];

	int n;
	cin >> n;

	for (int i = 0; i < n; i++) {
		cin >> a[i];
	}

	sort(a, a + n, compare);

	for (int i = 0; i < n; i++) {
		cout << a[i] << " ";
	}
